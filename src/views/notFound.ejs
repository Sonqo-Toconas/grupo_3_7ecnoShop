<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            background: rgb(139,143,154);
            background: linear-gradient(0deg, rgba(139,143,154,1) 0%, rgba(56,62,76,1) 50%);
            color: rgb(230, 243, 255);
            font-family: Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            
    margin-bottom: 100vw;
        }
        body h1, pre {
            text-align: center;
        }
        body img {
            border-radius: 5px;
            opacity: 0.8;
        }
        body button {
            background-color: rgb(58, 57, 57);
            color: white;
            border: none;
            border-radius: 5px;
            outline: none;
            width: 100px;
            height: 50px;
            cursor: pointer;
        }
        body div {
            display: flex;
            align-items: center;
            flex-direction: column;
        }
        body p {
            font-size: 5px;
        }
        @media (min-width: 600px) {
            
            body img {
                width: 300px;
                height: 300px;
            }
            body p {
                text-align: center;
                font-size: 1rem;
                margin-bottom: -50vw;
            }
            body {
                margin-bottom: 35vw;
            }
        }
        
        @media (min-width: 1100px) {
            body {
                margin-bottom: 35vw;
            }
        }
    </style>
    <title>Not Found</title>
</head>

<body>

    <div>
        <h1>La pagina no ha sido encontrada</h1>
    <a href="/">
        <img src="/images/telefono-triste.jpg"
        width="100vw" height="100vh" alt="NotFound">
    </a>
    </div>
    
    <div>
        <button onclick="showError('<%= JSON.stringify(errorMsg) %>')"> Mostrar Error</button>
        <div>
            <p id="errorMsg"></p>
        </div>
    </div>
    
    <script>
        function showError(data) {
            
            let errorMsg = document.querySelector('#errorMsg')
            if (errorMsg.textContent === '') {
                let body = document.querySelector('body')
                body.style.marginBottom = '50vw'
                errorMsg.textContent = data
            }else {
                errorMsg.textContent = ''
                body.style.marginBottom = '-500vw'
            }
        }

    </script>
</body>

</html>